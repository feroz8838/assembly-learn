{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build and Run",
            "type": "shell",
            "command": "nasm -f elf -o ${fileBasenameNoExtension}.o ${fileBasenameNoExtension}.asm  && ld -m elf_i386 -o ${fileBasenameNoExtension} ${fileBasenameNoExtension}.asm && gdb ${fileBasenameNoExtension}",
            "group": "build"
        }
    ]
}




